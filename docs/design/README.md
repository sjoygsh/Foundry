# Design documents

Per-subsystem design written **before** implementing anything non-trivial (development
rule 1). A design doc explains the shape of a system and why, so that implementation is
transcription rather than invention, and so a future session can tell intent from accident.

Not everything needs one. Write one when a subsystem has real structural choices, will be
depended on by several others, or will be hard to change later.

## Owed, in order

Written documents move to the table below. This one is a schedule, not an index.

**M8's design is written and fully implemented:** [`scripting.md`](scripting.md), written
2026-09-09 and supported by ADR-0028 and ADR-0029. All eight of its §16 steps are complete as
of 2026-09-12, the last of them the author guide written from outside the tree. **M9's
packaging/distribution design is now written:** [`distribution.md`](distribution.md),
supported by ADR-0031/0032 (ADR-0032 supersedes ADR-0030). Its §14 has eight steps, **all
implemented** (2026-09-12/13): bounded preferences and content defaults, origin-preserving
installed/user package loading, explicit runtime staging, generated attribution, retained
diagnostics, and the validated local macOS application/dSYM/zip plus the separately gated
Developer ID/notarization path. Step 8 adds the recipient guide and proves the exported helpers,
checksum-matched transfer, no-toolchain runtime, persistence, relocated read-only user mods and
diagnostics from an external consumer. Credentialed certification and a genuinely clean Mac
remain mandatory deferred work for the first public release; the ad-hoc artifact is not their
equivalent. M9 is complete.
M7's `public-abi.md` remains implemented in full.

M6's two documents are both written and both implemented: `ui.md`, and `debug-overlay.md`,
written 2026-09-07 and implemented in full the same day. The second was written after the first
rather than beside it, because what an inspector may ask `scene` for and how per-subsystem
timing is collected are a different subject with different invariants, and because that document
is better written once the shape of a panel is known rather than imagined — which is exactly how
it turned out: §11 answers `ui.md`'s open culling question with a convention the implemented
widget set already supports, and could not have been written before it existed.

Also owed, and unchanged: the job system's document, whenever `CLAUDE.md` §9's overdue entry is
re-dated.

## Written

| Document | Covers | Decisions worth knowing about |
| --- | --- | --- |
| [`distribution.md`](distribution.md) | M9 configuration/preferences, writable package roots, release staging, notices, diagnostics, macOS bundles and eight implementation steps | **Designed 2026-09-12; implemented in full 2026-09-13.** Step 1 adds the bounded `settings.fset` envelope and confined atomic replacement; Step 2 applies ordinary content defaults and user overrides; Step 3 combines installed/user roots while retaining package provenance; Step 4 stages exact declared runtime inputs with a stable SHA-256 inventory; Step 5 generates complete attribution; Step 6 retains bounded failure/session evidence; and Step 7 produces the validated `.app`, matching dSYM, ad-hoc local zip and separately credentialed Developer ID/notary path. Step 8 proves those helpers from an outside build, checksum-matched HTTP transfer, no-toolchain SDL3/Metal/audio runtime, cross-process preferences, a relocated read-only app with a precompiled user package, and failed-then-clean diagnostics. Strict ad-hoc integrity passed and Gatekeeper rejection was expected. ADR-0032 defers actual Developer ID signing, Apple notarization and the clean-recipient quarantined download to the first public release; it does not equate the ad-hoc artifact with one. |
| [`scripting.md`](scripting.md) | M8 runtime, package/source model, ABI v2 source access, bindings, sandbox limits, lifecycle, state migration and eight implementation steps | **Designed 2026-09-09; implemented in full 2026-09-12.** Restricted Lua 5.5.1 proves the protected runtime boundary; `foundry:script`, confined bounded/revisioned source loading, fpack derivation and manifest-v2 metadata work without Lua in content-only hosts; additive ABI v2 copies that source through the public contract while v1 remains frozen. Binding 1 publishes the bounded content, world and spawn surface over that table, with per-invocation budgets and script-owned entities. The package lifecycle holds one stable slot and one registered system per package, with activation, fault, teardown and structured diagnostics, and the sandbox ships a script the world's fixed tick drives. Candidate-VM reload then replaces a package's code under that slot: the old state crosses as a bounded value tree or through the module's own `migrate`, everything before the commit may fail without touching the VM, the ledger, the registration or the world, and the commit itself allocates nothing and runs no script code — so editing the sandbox's `.lua` beside the executable changes what it does on the next tick while its state and its entities stay. The completed adversarial matrix proves bounded failures, package isolation, deterministic fresh runs, confined-source recovery and live windowed bad-edit recovery. The author guide, `docs/modding/script-mods.md`, was then written by building a script package outside this repository and rebuilt from its own listings to check that it describes what the engine does. M8 is closed; §15's open questions stay open. |
| [`core-memory-and-handles.md`](core-memory-and-handles.md) | Allocator model, generational handles, content ID hashing, logging, assertions, math, time, RNG | Handles are `extern struct` because they cross the C ABI later; null handle is all-zero bits; FNV-1a 64 and PCG32 are **specified in the document**, not delegated to `std`, because both are persisted; simulation time is an integer tick count, never a float |
| [`rhi.md`](rhi.md) | Devices, resources, memory intent, resource states, the frame ring, command recording, pipelines and binding, the validation backend | Every decision taken from the **strictest** API, not the first one implemented: four bind groups because Vulkan only guarantees four, 128-byte inline constants because that is Vulkan's push-constant minimum, `device_local` resources are never mappable so unified memory cannot become a habit, and state transitions are declared at pass boundaries and enforced by the null backend. **Metal backend implemented 2026-09-04**; §9 gained the binding index convention it forced — which is shader-visible, and so a contract each future backend owes its own written version of |
| [`app-and-frame-loop.md`](app-and-frame-loop.md) | The engine loop, subsystem lifecycle, allocator ownership, the log sink, loading content packages | `Engine` is a library you drive, not a framework that calls you back; input is captured once per frame so every simulation step in it sees the same value; teardown is strictly reverse of initialisation; `alpha` is for the render only; the engine consumes a load order and does not compute one, loads package zero through the same call a mod's package uses (I3), and leaves the loaders to the game because it has no `render2d`; hot reload builds a whole new content set and swaps it, at the top of a frame, and publishes a generation counter because a handle survives a reload and anything derived from one does not. **§8 added 2026-09-05** |
| [`render2d.md`](render2d.md) | The game-facing 2D renderer: submission model, coordinate spaces, camera, sprites, batching, buffers, textures and atlases, text, statistics | The **first boundary with users we do not control**, so names here are compatibility decisions; immediate submission with retained resources, so `scene` is not duplicated and the ABI stays simple; `app` owns the frame and the game never sees a render pass; world Y is up and screen Y is down; the sort key is `(layer, submission_index)` and never texture, because reordering translucent sprites is wrong; colour is linear throughout; the renderer keeps **its own retirement queue** rather than trusting the RHI's unimplemented deferred destroy. **Implemented from M2 onward and grown since** — the batcher, camera, atlas and text at M2; the texture loader at M3; `drawTilemap` and `Tiles` at M5; `Region.cell` and the frame-selection functions with it. This document has no Resolution sections: what implementation settled is recorded in `PROJECT_STATE.md`'s milestone entries instead, which is a gap worth closing the next time the renderer is opened |
| [`content-schemas.md`](content-schemas.md) | Schemas, records, packages, the `.fdt` authoring format, the `.fpk` runtime format, load order and override semantics | `data` depends on **`core` alone**, so it cannot open a file — the parser is handed bytes and resolves `@import` through a caller-supplied callback, which makes the whole content pipeline a pure function and every test hermetic; two separate ID spaces for schemas and content; `core` refuses to normalise IDs, so `data` refuses to accept anything that would need normalising; content IDs are bare tokens rather than strings so a typo fails at compile time and `grep` finds every use; directives are `@`-prefixed so a future one can never collide with a mod's schema name. `.fpk` reads in place — explicit little-endian loads at schema-computed offsets, a presence bitmap per block, strings deduplicated and validated once — and its reader is tested by mutating a valid package one byte at a time; a package carries every schema its records use, so it can be read with nothing but itself, and the store reads each record against the copy its own package shipped rather than the registry's newer one. **Parser, schema registry, checker, `.fpk` and the store implemented 2026-09-04**; see its three Resolution sections for what implementation forced, including one decision the store reversed |
| [`assets.md`](assets.md) | Asset identity, ID derivation, the registry, runtime-registered loaders, hot reload | An asset is a content record and its identity is its content ID; a path *derives* an ID at compile time and `fpack` materialises it into the package, so no runtime code can resolve anything by path; derivation transforms nothing, for the same reason `core` refuses to normalise; `render2d` registers the texture loader into `asset` from above, so the dependency points down while the capability points up; the asset schemas live in `asset` so a content compiler can see them without linking a renderer, and a derived record is `.fdt` text put through the same parser and checker as an authored one; `source` is location and never identity, so a package's root is mounted with the registry rather than carried by `data`; a loader's payload is one opaque 64-bit word, which a handle fits in as well as a pointer; zero references means evictable and nothing evicts on a schedule; hot reload swaps behind the handle and a failed reload changes nothing, and nothing recompiles at runtime — `fpack` compiles, the engine reloads. **Implemented in full 2026-09-04 and 2026-09-05** (`asset/`, `tools/fpack/`, `render2d/loader.zig`, `app`); see its three Resolution sections |
| [`platform-interface.md`](platform-interface.md) | Window and surface, events and input, filesystem, dynamic libraries, clock, the null backend | Compile-time backend selection with a `comptime` conformance check; logical size and pixel size are never conflated; input is captured into a per-frame immutable snapshot (an I9 requirement); keys are identified by physical position, with text entry as a separate event. **Implemented 2026-09-03**, both backends — see its Resolution for what implementation changed (chiefly the `Os`/`Platform` split and `std.Io` stopping at this layer) and what SDL3 needed absorbing |
| [`entity-storage.md`](entity-storage.md) | The `scene` module: entities, type-erased component storage, queries, systems, entities in content, world saves | A component type **is a schema**, so identity, versioning, checking and serialization are the ones `data` already has, and a mod declares a component type with `@schema`; the handle is called `Entity` because an entity has no other form; queries iterate the **first** named component's dense array rather than the smallest, so order is a property of the query rather than of the data (I9); a component instance in content is its own record, so a mod overrides one component without restating the entity; a save preserves entity identity exactly — slots, generations and the free list — which is what lets an `Entity` inside component data serialize as a handle with no remap pass, and what makes a reload resume in the iteration order it was saved in; `scene` can read neither a clock nor input, so input reaches systems as data the game wrote down. **All six steps implemented 2026-09-05**, closing M4 (`engine/src/scene/`, and `.fsav` in `save.zig`); see its seven Resolution sections for what that settled, chiefly a component type being identified by a handle rather than an index so a future mod unload cannot leave dangling references, an asset reference in a component being a content id rather than an `AssetHandle` — the same fact as `scene` needing no `asset` dependency — a world being unable to borrow the engine's schema registry because hot reload replaces it, and, following from that, a spawn having to check that a package's schema agrees with the registered one before position-matched deserialization reads the right bytes into the wrong field |
| [`tilemaps-and-collision.md`](tilemaps-and-collision.md) | The `physics2d` module (bodies, shapes, tile grids, broadphase, movement and queries), tilemaps as content, and tilemap rendering | Three things get called "tilemap" and this separates them: a grid of numbers, drawing it, and colliding against it. `physics2d` is **L1 on `core` alone** and holds **no time and no velocity** — a caller says *move this by that* and is told where it stopped, so movement feel stays in the game and the module has no clock to read; the grid is a first-class shape source rather than ten thousand boxes, which is what makes **neighbour-aware face culling** possible and the internal-edge snag exact rather than tolerated; the spatial hash may bucket how it likes but candidates are sorted by handle before use, so the acceleration structure can be replaced without changing a result (I9); a body carries an opaque `u64` rather than an `Entity`, which is the whole of the coupling to `scene`; the grid's bulk is an **asset** because a list of ten thousand integers in `.fdt` is a binary payload in disguise; and M5 adds **no engine-owned component types**, because a `foundry:collider` invented now is a name every mod is stuck with from M7. **Fully implemented, steps 1-7, 2026-09-05/06** (`engine/src/physics2d/`, `engine/src/asset/tilemap.zig`, `engine/src/render2d/tilemap.zig`, and the wiring in `samples/sandbox/`); see its five Resolution sections for what that settled, chiefly that the broadphase's contract is "no false negatives" rather than "the same candidate set" — a larger cell legitimately returns more candidates, so the invariance claim belongs one level up at `World.queryBounds` — that a body is read through a const pointer because a caller moving one by assignment would leave the broadphase describing where it used to be, that the four pair tests collapsed into **one**: every pair reduces to a rounded box and the reduction composes, so box-box, circle-circle, box-circle and a raycast are one static test and one swept test asked different questions, and face culling extends to a corner by requiring both of its faces; that the tilemap schemas and the grid loader live in **`asset`, not `render2d`** — §13's open question 1 answered early by its own trigger, because `fpack` has to check a map without linking a renderer; that `fpack` now emits **two** things, the `.fpk` and a tree of compiled assets, because a tile grid is the first asset kind whose authoring and runtime forms are different files; and that drawing is one pure iterator plus a loop, with the design's `view` parameter dropped because `setView` already names a space for every other draw, culling half-open so a screen-aligned camera does not draw an extra row and column forever, and `foundry:tilemap.layer` grown an optional `empty` at version 2 because a second layer with no way to say *nothing here* paints over the first; and that the last step added **nothing to the engine at all** — a collision world, a `sandbox:collider` and about two hundred lines, all of them in the sample, because §11's "the game wires them" was a claim and this is it being paid. **Paid twice as of 2026-09-06**: `samples/room` wrote the same two hundred lines again, near-identically, which turns "the game wires them" from a design position into evidence that *every* game will — the argument for a `render2d`-side helper, left unmade because what shape it takes belongs to the milestone that makes it |
| [`audio.md`](audio.md) | The audio device in `platform`, the mixer and voices in the new `audio` module, WAV decoding and `foundry:sound` in `asset` | There is a **second thread, Foundry did not create it, and it has a deadline**, and every decision here is downstream of that: state is split by which thread owns it so no field is written by both, two SPSC rings carry commands out and retirements back, and no lock appears anywhere. The device speaks `f32` interleaved and that is not negotiated — a sample-format enum would put a `switch` in the mixer's inner loop for a case that exists because a driver is old; rate and channel count are, because they change what the mixer computes. **The null device is stepped, not threaded**, and the document is explicit that this proves the arithmetic and the protocol but *not* the ring under real concurrency. A `VoiceHandle`'s generation is the answer to stopping a footstep that already ended and killing the door that took its slot. Two lifetime hazards get two mechanisms: the mixer holds the asset reference for a voice's life, and it keeps **its own retirement** — the same answer `render2d` gave for GPU resources — so hot reload cannot free samples out from under the callback. `scene` and `audio` are both L3 and neither depends on the other, so a system *cannot* reach the mixer: that is I9's emit-never-observe rule enforced by the build rather than by documentation. **The mixer opens the device**, so `app` gains no dependency on audio at all. **Fully implemented, steps 1-6, 2026-09-06** (`engine/src/audio/`, `engine/src/platform/audio.zig` and both backends, `engine/src/asset/wav.zig` and `sound.zig`, `engine/tests/sound_pipeline.zig`, and the wiring plus three generated `.wav`s in `samples/sandbox/`); see its four Resolution sections for what that settled, chiefly that a full command ring makes `play` **return an error and undo its claim** where every other dropped command is merely counted, because a `play` that vanished would strand a slot no retirement is ever coming for; that the retirement ring is sized to the voice count, which makes dropping one **structurally impossible** rather than unlikely, so the push asserts instead of ignoring its result; that a one-shot ends in the buffer it runs out in rather than the one after, which is what makes the lifetime rule sayable; that the WAV decoder asks **the format before the bit depth**, because four bits is nonsense for PCM and correct for ADPCM and the author deserves the right sentence; that the SDL3 backend **adopts the device's own sample rate** and fills whole blocks into the stream's queue, so Foundry's callback always gets the block size it was promised; that pan is **ignored on a mono device** rather than summed, which would make a centred sound quieter than a panned one; and that teardown needed a third call — `shutdown`, between the loader going and `deinit`, because the registry unloads through a loader that borrows the mixer while the mixer holds a reference per playing voice, and the two requirements want opposite orders |
| [`ui.md`](ui.md) | The `ui` module: the immediate-mode kernel, identity, input and capture, layout, the draw list, style, the walker into `render2d`, and the debug widget set | The first subsystem whose state is a **memory of what the user was doing a frame ago**, and the first that **competes with the game for input**. `ui` is **L1 on `core` and `platform`** and emits a draw list rather than calling a renderer (ADR-0024), which is what lets the whole interaction model unit-test with no device, no window and no frame; the price is two draw vocabularies and a walker in `app`, whose one real hazard — the kernel measuring text differently from the renderer drawing it — is answered by a single sanctioned `BitmapFont` → `FontMetrics` conversion and a test that measures a fixed corpus both ways. **Identity and display text are separate parameters**, refusing the label-as-id convention every immediate-mode UI uses, because Foundry's game UI is translated content and deriving ids from on-screen strings resets every scroll position and focus the moment the language changes — a bug that only appears in the language nobody tests. A UI id is **not** a `ContentId` and uses a seeded hash local to `ui` so the resemblance cannot become a shared guarantee: one is frozen and reaches save files, the other is runtime-only and free to change. `hot` is decided from the **previous** frame's rectangles, because a widget's bounds are known only after the caller has already asked whether it was clicked. Capture is **advisory, not enforced** — the kernel cannot filter the game's input without inverting the layering, which is the same rule `app-and-frame-loop.md` already applies to events. Strings in the draw list are **copied into a per-frame arena**, because the obvious way to draw a number is to format into a stack buffer and a borrowed slice into one is dangling by the time the walker runs. Style is a value the kernel only ever reads, so the content-driven game theme is a new producer rather than a rewrite. `render2d` gains exactly two things — `setClip` (exposure and a batch break; the RHI already has `setScissor` on both backends) and `blankRegion` — and the evidence the second was right is that **both samples delete the white texture they each hand-rolled identically**. **Fully implemented, steps 1-6, 2026-09-06/07** (`engine/src/ui/`, `engine/src/app/ui_draw.zig`, `engine/tests/ui_text.zig`, and the overlay in `samples/sandbox/` plus the card in `samples/room/`); see its six Resolution sections for what that settled, chiefly that `hot`'s deferral is about **overlap** and not about when a rectangle is known, which the document had explained wrongly; that a container takes the pointer **without entering the hot/active model**, because a panel competing for `hot` would steal the press meant for the control the pointer had just moved onto; that `wantsKeyboard` must mean **consuming typing** rather than holding focus, since a press focuses any widget and a game would otherwise lose its movement keys to a slider drag — found by building the game, because no kernel test knows that a slider is not a text field; that a panel closing itself **mid-frame hands the click back to the game**, so a widget records and the caller acts after `end`; that the walker takes an `app.UiFont` pairing a font with its spacing, which makes the measurement drift §8 feared **structurally impossible** rather than merely tested; and that what a game holds back is the bindings a keyboard **types** — letters, digits and the keys that edit them, never escape and never a function key |
| [`sprite-animation.md`](sprite-animation.md) | Clips as content, animation state as a component, and the frame-selection and region-slicing the engine provides | The crossing between `scene` and `render2d` that this document was written to resolve **dissolves once the state is an integer**: `scene` holds a tick count and a frame index and never learns what a texture is, `render2d` turns an index into a region and never learns what an entity is, and the layering could not have let them meet anyway (I7). A float accumulator is refused for three reasons and the deciding one is that it makes "which frame at tick 700?" only *nearly* answerable — the failure that is right in nineteen tests out of twenty; `elapsed_ticks` is chosen over `started_at_tick` because pause and restart are then arithmetic rather than fixups. The component stores a frame **index, not a `Region`**, because a `Region` carries a `TextureHandle` and a component is serialized — the same rule ADR-0021 already applied to textures. The engine's whole contribution is two pure integer functions plus a grid cut built on `Region.sub`; the clip schema and the component are the sample's, for the same reason M5 adds no `foundry:collider`. **Fully implemented, steps 1-3, 2026-09-06** (`engine/src/render2d/animation.zig`, `Region.cell` in `atlas.zig`, `engine/tests/sprite_animation.zig`, and the wiring in `samples/sandbox/`); see its two Resolution sections for what that settled, chiefly that an out-of-range grid cut is **empty rather than clamped** because a stuck animation is indistinguishable from a non-looping clip working while a vanished one sends its author to the clip; that a frame held for zero ticks is never shown and pinning therefore **clamps the tick rather than the index**, so looping and pinning are one walk that settles on the last frame actually displayed; that a clip's numbers reach the system as a **table resolved once per content generation**, because a system is handed the tick and nothing else and so cannot read a record — which is also why advancing a clip *is* a system where moving the player is not; that a looping clip **wraps its own elapsed count by the clip's duration** and a one-shot saturates, with the wrap asserted to be invisible; and that the reload check is an integration test comparing **the UVs a draw call would carry, bit-exactly**, because an epsilon there would pass for the float accumulator §4 refuses |
| [`debug-overlay.md`](debug-overlay.md) | The `debug` module: the frame profiler, per-allocator memory reporting, the log ring and console, the entity inspector, the content browser, and the introspection APIs in `core`, `data`, `asset`, `scene` and `app` beneath them | **Written 2026-09-07; all six steps of §17 implemented the same day** (`core/profile.zig`, `Engine.beginScope` and the engine's seven spans; `core.mem.Counted`, `Arena.highWater` and the engine's memory registry; the log ring in `app/log_sink.zig` and the sandbox's console over it; the introspection calls in `scene`, `data` and `asset`; and `engine/src/debug/` itself — the overlay, the panel registry and five panels, with the sandbox deleting its hand-built ones and registering one of its own). ADR-0025 is **accepted**; step 5 is what created `debug` and put it in the layering graph. The milestone's value is the introspection, not the panels: those names reach mods at M7, so each is a compatibility decision, and the overlay's rule is that it may use **no call the ABI could not expose** — I3's argument applied to tooling, so that the editor at M6+ is a re-host rather than a rewrite. **The clock stays above the subsystems**: a recorder carrying a clock handed down to `scene` would undo the exact ADR-0007 property I9 depends on, so timing is collected at call sites by callers that already have a clock, and per-system detail stays an open question rather than being bought by perforating the layering. **A component is read the way a save reads it** — serialized through the type's own function into a block and read back as values — because a component's in-memory layout is a Zig struct's and only its schema is public, so casting bytes would be an answer that looks right until the first mod-defined component; a type with no serializer is not inspectable and says so, which `Registration.savable()` already names. The store does not keep the definition an override replaced and does not start: every package's bytes are retained anyway, so **the override chain is reconstructed on demand** from the packages' own record tables. The log ring is a *second* sink and never replaces stderr, keeps its own level so quietening the terminal does not blind the console, writes every line contiguously so a widget can be handed one slice, and makes the mixer's existing "the audio callback never logs" into a rule something now depends on. Five modules gain **nothing at all**, which is the strongest thing §9's table says. §11 closes `ui.md`'s culling question: the caller emits only visible rows, because a row is a fiction only the caller maintains, and the kernel could not have skipped the formatting anyway. **Step 1's Resolution** records what it settled: the design's six engine spans are seven, because on Metal the vsync wait is at *drawable acquisition* and not at present; the profiler perturbs the null backend's synthetic clock, which advances per reading, so clock readings are shared where the frame already makes one and a deterministic headless run turns the profiler off; a dropped span needs a stack sentinel so its `close` does not close its parent; and the first profile raised a suspicion (`render.prepare` at 63% of a debug frame) that an optimised build then killed (1.07ms, and the frame 82% waiting for the display). **Step 2's** corrects §5 — the engine does not wrap its own allocator, because a wrapper made inside `init` arrives after the struct has already allocated through the raw one; counting the engine is one line at the call site instead — and records that counting is what reveals which allocator a shared container belongs to, plus a thirteen-fold engine-memory difference between the null and Metal builds that the null backend's CPU-side buffer storage explains. **Step 3's** corrects §6.1 — a ring filtered by the terminal's level goes quiet exactly when somebody quietens the terminal, so the two destinations are decided independently — and records that the ring is statically sized because an ambient thing has no owner to hand it an allocator, that it spins rather than taking a `std.Io.Mutex` because `logFn` has no `Io` to ask for, and two bugs a shape-checking test missed: a `0xff` "off" sentinel that captured everything, and an empty-ring reservation that never advanced the head so every line overwrote the last. **Step 4's** renames two calls that collided with the field they enumerate, records that a green `zig build check` covered a body Zig had not analysed because nothing called it yet, and notes that a schema knows its id and not its spelling — so a browser asks the store for a name **Step 5's** records that ADR-0025's dependency list was three modules too long and was trimmed by the ADR's own rule; that `Sources` carries what the engine does *not* own, because listing a store beside the `*Engine` gives a content panel two answers to "which store"; that panels are handed a `Frame` snapshot rather than the engine, which makes every panel non-generic, every panel test engine-free and `reloadContent` a bound pointer pair that is literally the ABI's shape; that `beginScroll` places itself where it is told and never advances the cursor, which had already put the sandbox console's footer on top of its own first row; that the log ring is only reachable when the *root* source file installs `std_options`; and the two numbers the first windowed run produced — `debug.overlay` at 0.21ms *inside* the profile it draws, and **31 batches**, which is step 6's subject **Step 6's** is M6's exit criterion, executed: `samples/room` adopted the overlay for one import and one key, and the overlay's own batch cost was diagnosed with the overlay — 32 batches with five panels open, of which **29 of 31 breaks involve a texture change** (the suspicion `ui.md` recorded three times and never measured) and 2 are a clip change alone (a second cause nobody had written down, because a batch cannot span a clip either). The test walks the draw list the way `app.drawUi` does and asserts that counting runs of identical `(texture, clip)` reproduces `frameStats().batches` **exactly**, which is what makes the split arithmetic rather than a story; one texture behind both would leave **12**, computed by the same walk. The fix is left to `render2d` with its value measured first (rule 2), because the other half of the answer is that describing five panels costs 0.21ms of a 7.7ms frame — the overlay costs draw calls, not time |
| [`public-abi.md`](public-abi.md) | The `abi` module: the entry point, `FoundryApi_v1`, the types that cross, result codes, enumeration and callbacks; and the `mod` module: manifests, discovery, dependency resolution and load order | **Written 2026-09-07; all seven steps of §19 implemented by 2026-09-09, closing M7.** `mod` at L2, and `abi` at L5 with the complete 135-call `FoundryApi_v1`, including scene, rendering, UI, audio and collision, plus the native lifecycle. The only signature frozen forever is `foundry_mod_init(get_api, self)` — a *query function* rather than the table, which is what makes ADR-0004's "added alongside, never replacing" implementable at all; every pointer the API hands out is borrowed until the mod returns control, so no call in `_v1` transfers ownership in either direction; a cursor rather than an index, carrying a generation, so a mutated container is detected rather than resynchronised; a component is read through its schema, except for a type the calling mod itself registered, where raw bytes are correct because the mod owns the layout. Load order is a **stable** topological sort — the player's order is real intent and survives wherever the graph permits, with content id as the final tie-break. **The one engine change the boundary forces** is `scene`'s mutation guard, which asserts on a premise ("the caller is engine or game code") the ABI falsifies — the instance ADR-0025 predicted and named as its own falsification test |
